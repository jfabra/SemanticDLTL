---
name: Bug report
about: Something does not work as documented
title: ""
labels: bug
---

**Version**: output of `dltl-mc --version`, and your Python version.

**Minimal model** (a few lines of a `.mod` file, including its header):

```
aE,nV
id0,a&1
```

**Formula or command**, exactly as typed (attach the propositions file if it uses `PROP.` or `_LOAD`):

```
F x.("(x)x[V] == 1")
```

**What happens** (the printed line, or the error and its traceback):

**What you expected**:
